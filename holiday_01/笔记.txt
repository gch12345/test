1.当局部变量没有赋初值的时候，那么它的值是随机值。当全局变量没有赋初值的时候，其实它会自动赋初值为0.
2.1 sizeof(数组名)，计算整个数组的大小，sizeof内部单独放一个数组名，数组名表示整个数组。 
2.2 &数组名，取出的是数组的地址。&数组名，数组名表示整个数组。
3.在C语言中，函数返回值类型的定义缺省，此时函数返回值的隐含类型为 int 。
4. 结构体总大小为最大对齐数（每个成员变量都有一个对齐数）的整数倍。 
5.